<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.apnatutorials.androidprogressdialog.MainActivity">

    <Button
        android:id="@+id/btnIndeterminateCircular"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="progressBarDemo"
        android:text="Indeterminate progress dialog(Circular animation)" />

    <Button
        android:id="@+id/btnIndeterminateHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnIndeterminateCircular"
        android:onClick="progressBarDemo"
        android:text="Indeterminate progress dialog(Circular animation)" />

    <Button
        android:id="@+id/btnDeterminateHorizontalProgressDialog"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnIndeterminateHorizontal"
        android:onClick="progressBarDemo"
        android:text="Determinate progress dialog( Horizontal )" />

    <Button

        android:id="@+id/btnProgressBarWidget"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnDeterminateHorizontalProgressDialog"
        android:onClick="progressBarDemo"
        android:text="Determinate progress bar widget( Horizontal )" />

    <ProgressBar
        android:id="@+id/progressBar"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:max="100" />


</RelativeLayout>
